# Rust
target/
*.pdb
# Cargo.lock is committed on purpose: this workspace ships binaries
# (server, demo-cli), and reproducible builds want a pinned lockfile.

# SQLite
*.db
*.db-shm
*.db-wal

# Environment
.env
.env.local
.env.*.local

# SvelteKit / Node
frontend/node_modules/
frontend/.svelte-kit/
frontend/.vite/
frontend/build/
frontend/dist/
frontend/.env
frontend/.env.*

# Demo identity files written by demo-cli — contain PRIVATE KEYS in cleartext.
# Regenerable via `cargo run -p demo-cli -- scenario`. Never commit them.
/*.json

# Editor backups
*.bak

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
